<Window x:Class="Restaurant.Client.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="800" Width="1280" WindowStyle="SingleBorderWindow">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="241*"/>
            <ColumnDefinition Width="15*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Height="26" Margin="2,2,2,2" FontWeight="Bold" FontSize="15" Grid.ColumnSpan="2">
            <MenuItem Header="Рестораны" Click="OpenRestaurantPage_Click"/>
            <MenuItem Header="Столики" Click="OpenTablesPage_Click"/>
            <MenuItem Header="Выход" Click="CloceProgramm_Click"/>
        </Menu>
        <Separator Grid.Row="1" VerticalAlignment="Top" Grid.ColumnSpan="2" Margin="0,2,0,0"/>

        <DockPanel Margin="3,3,3,3" Grid.Row="1" Grid.ColumnSpan="2">
            <Frame x:Name="MainPageZone" NavigationUIVisibility="Hidden"/>
        </DockPanel>
    </Grid>
</Window>
